import 'package:flutter/material.dart';

class Tab2Data extends StatelessWidget {
  const Tab2Data({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
