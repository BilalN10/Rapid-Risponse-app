import 'package:flutter/material.dart';

class SmartButtonIndicatorWidget extends StatelessWidget {
  const SmartButtonIndicatorWidget({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      //size: 40.0,
    );
  }
}
